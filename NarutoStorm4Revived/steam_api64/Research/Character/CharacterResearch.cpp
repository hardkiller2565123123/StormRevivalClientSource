#include "CharacterResearch.h"
namespace StormRevival { namespace Research { namespace Character {
    const char* Summary()
    {
        return "Character: 216-byte, 96-byte, 72-byte, 64-byte, and 104-byte table loaders are mapped; filenames still need xrefs.";
    }
}}}
