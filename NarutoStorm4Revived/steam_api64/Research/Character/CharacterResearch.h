#pragma once
namespace StormRevival { namespace Research { namespace Character {
    // Character/variant research:
    // sub_1401C9CFC generates keys using %s%04d.
    // Several off_1413B/off_141485 database paths still need xrefs to name exact XFBIN files.
    const char* Summary();
}}}
