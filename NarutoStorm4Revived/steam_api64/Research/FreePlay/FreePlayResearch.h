#pragma once
namespace StormRevival { namespace Research { namespace FreePlay {
    // Free battle / scenario setup is tied to gpSetting.xfbin through sub_14043FB28.
    // sub_14043F208 parses battle script path, battleParam, players, characters, spawn position, direction, UI, and file lists.
    const char* Summary();
}}}
