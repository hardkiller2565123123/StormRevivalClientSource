#include "OnlineMenuResearch.h"
namespace StormRevival { namespace Research { namespace OnlineMenu {
    const char* Summary()
    {
        return
            "OnlineMenu: Flash bridge, callback registration, main menu init, lobby init, "
            "GetMode/GetModeBattele, banner/title, and lobby ready callbacks are mapped.";
    }
}}}
