#pragma once
namespace StormRevival { namespace Research { namespace Settings {
    // Settings here means game/scenario settings, not the custom Online Menu Settings page.
    // The key database is data/gp/setting/gpSetting.xfbin.
    const char* Summary();
}}}
